name: personalize_time_management
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  line_awesome_flutter:
  google_fonts:

  dotted_border: ^2.1.0
  fl_chart: ^0.64.0
  timeline_tile: ^2.0.0

  flutter_neat_and_clean_calendar: ^0.3.0+12

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true